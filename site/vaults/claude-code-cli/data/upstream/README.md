# The published record this vault is checked against

`delta.json` is the stored delta for this shape and starting mandate as published at abp.sgit.ai v0.3.0 (computed 2026-09-11T13:00:37Z by abp.delta/v1). The build recomputes the delta from `data/grant.json` and `data/mandate.json` and refuses to write if any of the six lists disagrees with this file. Once the mandate is corrected the two will differ on purpose; delete this file then, or keep it as the record of the draft the correction started from.
